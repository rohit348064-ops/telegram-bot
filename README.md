# Telegram Referral Bot 💸
A simple auto-verify, referral and withdrawal Telegram bot by Subhash.

### Setup Steps:
1. Add your bot token in `token.txt`
2. Install dependencies:
   ```bash
      pip install -r requirements.txt